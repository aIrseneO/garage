{
  "mcpServers": {
    "gitlab": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--network=jenkins_jenkins",
        "--rm",
        "garage/jenkins:mcpgitlab"
      ]
    }
  }
}
